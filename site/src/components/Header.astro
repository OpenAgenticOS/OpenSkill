---
import type { Locale } from "../types";
import { t } from "../lib/i18n";

interface Props {
  locale: Locale;
  active?: "skills" | "workflows" | "recipes" | "guide";
}

const { locale, active = "skills" } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const localePathSuffix = active === "skills" ? "" : `${active}/`;

const navItems = [
  { key: "skills" as const, label: t("navSkills", locale), href: `${base}/${locale}/` },
  { key: "workflows" as const, label: t("navWorkflows", locale), href: `${base}/${locale}/workflows/` },
  { key: "recipes" as const, label: t("navRecipes", locale), href: `${base}/${locale}/recipes/` },
  { key: "guide" as const, label: t("navGuide", locale), href: `${base}/${locale}/guide/` },
];
---

<header class="sticky top-0 z-30 border-b border-gray-200 bg-white/80 backdrop-blur dark:border-gray-800 dark:bg-gray-950/80">
  <div class="mx-auto flex max-w-7xl items-center gap-3 px-4 py-3">
    <a href={`${base}/${locale}/`} class="flex items-center gap-2 shrink-0">
      <span class="text-xl">🧠</span>
      <span class="text-lg font-bold tracking-tight">OpenSkill</span>
    </a>
    <span class="hidden sm:inline text-sm text-gray-500 dark:text-gray-400">
      {locale === "zh" ? "企业大模型技能库" : "Enterprise AI Skill Library"}
    </span>

    <nav class="ml-4 hidden md:flex items-center gap-1">
      {navItems.map((item) => (
        <a
          href={item.href}
          class:list={[
            "rounded-md px-3 py-1.5 text-sm font-medium transition",
            active === item.key
              ? "bg-blue-50 text-blue-700 dark:bg-blue-950 dark:text-blue-300"
              : "text-gray-600 hover:bg-gray-100 dark:text-gray-400 dark:hover:bg-gray-800",
          ]}
        >{item.label}</a>
      ))}
    </nav>

    <div class="ml-auto flex items-center gap-2">
      <slot name="search" />
      <div class="flex rounded-lg border border-gray-200 text-sm dark:border-gray-700">
        <a
          href={`${base}/en/${localePathSuffix}`}
          class:list={["px-2.5 py-1 rounded-l-lg transition", locale === "en" ? "bg-blue-600 text-white" : "hover:bg-gray-100 dark:hover:bg-gray-800"]}
        >EN</a>
        <a
          href={`${base}/zh/${localePathSuffix}`}
          class:list={["px-2.5 py-1 rounded-r-lg transition", locale === "zh" ? "bg-blue-600 text-white" : "hover:bg-gray-100 dark:hover:bg-gray-800"]}
        >中文</a>
      </div>
      <a href="https://github.com/OpenAgenticOS/OpenSkill" target="_blank" rel="noreferrer" class="hidden sm:block rounded p-1.5 hover:bg-gray-100 dark:hover:bg-gray-800" aria-label="GitHub">
        <svg class="h-5 w-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 .5C5.37.5 0 5.78 0 12.292c0 5.211 3.438 9.63 8.205 11.188.6.111.82-.254.82-.567 0-.28-.01-1.022-.015-2.005-3.338.711-4.042-1.582-4.042-1.582-.546-1.361-1.333-1.723-1.333-1.723-1.089-.73.083-.716.083-.716 1.205.083 1.838 1.215 1.838 1.215 1.07 1.802 2.807 1.281 3.492.98.108-.763.418-1.282.762-1.577-2.665-.295-5.466-1.309-5.466-5.827 0-1.287.465-2.339 1.228-3.164-.123-.297-.532-1.497.117-3.12 0 0 1.001-.315 3.28 1.209a11.49 11.49 0 0 1 3-.396 11.49 11.49 0 0 1 3 .396c2.278-1.524 3.276-1.209 3.276-1.209.652 1.623.243 2.823.12 3.12.765.825 1.226 1.877 1.226 3.164 0 4.53-2.805 5.528-5.475 5.818.43.364.814 1.084.814 2.184 0 1.576-.014 2.846-.014 3.232 0 .316.216.684.825.568C20.565 21.917 24 17.5 24 12.292 24 5.78 18.627.5 12 .5Z"/></svg>
      </a>
    </div>
  </div>
  <!-- Mobile nav -->
  <div class="flex md:hidden border-t border-gray-100 dark:border-gray-800">
    {navItems.map((item) => (
      <a
        href={item.href}
        class:list={[
          "flex-1 py-2 text-center text-xs font-medium transition",
          active === item.key
            ? "text-blue-700 border-b-2 border-blue-600 dark:text-blue-300"
            : "text-gray-500 hover:text-gray-700 dark:text-gray-400",
        ]}
      >{item.label}</a>
    ))}
  </div>
</header>
