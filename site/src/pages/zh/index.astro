---
import Base from "../../layouts/Base.astro";
import Header from "../../components/Header.astro";
import SearchableGrid from "../../components/SearchableGrid";
import { getSkills } from "../../lib/data";

const skills = getSkills("zh");
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<Base
  title="OpenSkill — 企业大模型技能库"
  description="浏览 154+ 企业级 AI 技能，按岗位分类。一键复制系统提示词到 ChatGPT、Claude、Gemini。"
  locale="zh"
>
  <Header locale="zh" active="skills" />
  <div class="flex flex-1" style="min-height: calc(100vh - 57px);">
    <SearchableGrid client:load skills={skills} locale="zh" base={base} />
  </div>
</Base>
